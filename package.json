{
  "name": "geodynamo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "report": "bun run index.ts",
    "report:ci": "bun run index.ts --output geodynamo-report.md --json-output geodynamo-snapshot.json --field-output geodynamo-field-map.json --plan-output geodynamo-action-plan.json",
    "check": "bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.142.0"
  }
}
